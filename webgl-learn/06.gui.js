import { GUI } from  'three/addon/libs/lil-gui.module.min.js'

// const gui = new GUI();

// gui.domElement.style.right = 0;

const obj = {
    x: 30,
    y: 80,
    z: 0,
}; 

// gui.add(obj, 'x', 0, 100)
// gui.add(obj, 'y', 0, 100)
// gui.add(obj, 'z', 0, 100)